root@host127:/mnt/share/x30084275/accuracy_triton0811/xyz-triton-test-0725# pytest -sv /mnt/share/x30084275/accuracy_triton0811/xyz-triton-test-0725/accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py
================================================================================================= test session starts =================================================================================================
platform linux -- Python 3.12.13, pytest-8.3.2, pluggy-1.6.0 -- /usr/local/python3.12.13/bin/python3
cachedir: .pytest_cache
rootdir: /mnt/share/x30084275/accuracy_triton0811/xyz-triton-test-0725/accuracy_test/strict_ut
configfile: pytest.ini
plugins: xdist-3.6.1, anyio-4.14.2
collecting ... INFO 08-19 19:50:42 [__init__.py:52] Available plugins for group vllm.platform_plugins:
INFO 08-19 19:50:42 [__init__.py:54] - ascend -> vllm_ascend:register
INFO 08-19 19:50:42 [__init__.py:57] All plugins in this group will be loaded. Set `VLLM_PLUGINS` to control which plugins to load.
INFO 08-19 19:50:42 [__init__.py:237] Platform plugin ascend is activated
INFO 08-19 19:50:42 [platform.py:61] Breakable cudagraph is force disabled on Ascend because DeepSeek V4 PIECEWISE cudagraph is not supported yet.
collected 73 items                                                                                                                                                                                                    

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-128-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-128-4] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-128-8] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-8192-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-8192-4] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-8192-8] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-129280-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-129280-4] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-129280-8] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-248320-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-248320-4] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-248320-8] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-128-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-128-4] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-128-8] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-8192-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-8192-4] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-8192-8] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-129280-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-129280-4] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-129280-8] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-248320-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-248320-4] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-248320-8] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-128-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-128-4] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-128-8] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-8192-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-8192-4] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-8192-8] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-129280-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-129280-4] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-129280-8] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-248320-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-248320-4] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-248320-8] FAILED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-1-128-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-1-128-4] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-1-128-8] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-1-8192-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-1-8192-4] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-1-8192-8] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-1-129280-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-1-129280-4] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-1-129280-8] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-1-248320-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-1-248320-4] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-1-248320-8] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-2-128-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-2-128-4] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-2-128-8] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-2-8192-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-2-8192-4] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-2-8192-8] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-2-129280-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-2-129280-4] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-2-129280-8] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-2-248320-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-2-248320-4] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-2-248320-8] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-3-128-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-3-128-4] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-3-128-8] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-3-8192-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-3-8192-4] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-3-8192-8] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-3-129280-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-3-129280-4] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-3-129280-8] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-3-248320-1] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-3-248320-4] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[False-3-248320-8] PASSED
accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_all_neg_inf PASSED

====================================================================================================== FAILURES =======================================================================================================
____________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-1-128-4] _____________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192e660>, num_reqs = 4, vocab_size = 128, num_speculative_steps = 1, has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 2.8441436290740967 but got 3.5551793575286865.
E                       Absolute difference: 0.7110357284545898 (up to 1e-05 allowed)
E                       Relative difference: 0.24999993712907728 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
____________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-1-128-8] _____________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192e6f0>, num_reqs = 8, vocab_size = 128, num_speculative_steps = 1, has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 2.2968921661376953 but got 2.871115207672119.
E                       Absolute difference: 0.5742230415344238 (up to 1e-05 allowed)
E                       Relative difference: 0.25 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
____________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-1-8192-4] ____________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192e840>, num_reqs = 4, vocab_size = 8192, num_speculative_steps = 1, has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.544541835784912 but got 4.43067741394043.
E                       Absolute difference: 0.8861355781555176 (up to 1e-05 allowed)
E                       Relative difference: 0.2500000336317908 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
____________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-1-8192-8] ____________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192e900>, num_reqs = 8, vocab_size = 8192, num_speculative_steps = 1, has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.5005202293395996 but got 4.375650405883789.
E                       Absolute difference: 0.8751301765441895 (up to 1e-05 allowed)
E                       Relative difference: 0.2500000340547352 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
___________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-1-129280-4] ___________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192ea20>, num_reqs = 4, vocab_size = 129280, num_speculative_steps = 1
has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.6153392791748047 but got 4.519174098968506.
E                       Absolute difference: 0.9038348197937012 (up to 1e-05 allowed)
E                       Relative difference: 0.25 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
___________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-1-129280-8] ___________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192eab0>, num_reqs = 8, vocab_size = 129280, num_speculative_steps = 1
has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.611283302307129 but got 4.514103889465332.
E                       Absolute difference: 0.9028205871582031 (up to 1e-05 allowed)
E                       Relative difference: 0.24999993397954157 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
___________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-1-248320-4] ___________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192f8c0>, num_reqs = 4, vocab_size = 248320, num_speculative_steps = 1
has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 4.854004859924316 but got 6.067505836486816.
E                       Absolute difference: 1.2135009765625 (up to 1e-05 allowed)
E                       Relative difference: 0.24999995088208893 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
___________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-1-248320-8] ___________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192f950>, num_reqs = 8, vocab_size = 248320, num_speculative_steps = 1
has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.967859983444214 but got 4.959825038909912.
E                       Absolute difference: 0.9919650554656982 (up to 1e-05 allowed)
E                       Relative difference: 0.2500000150218619 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
____________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-2-128-4] _____________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192fa70>, num_reqs = 4, vocab_size = 128, num_speculative_steps = 2, has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 2.6376774311065674 but got 3.2970967292785645.
E                       Absolute difference: 0.6594192981719971 (up to 1e-05 allowed)
E                       Relative difference: 0.24999997740260274 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
____________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-2-128-8] _____________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192fb00>, num_reqs = 8, vocab_size = 128, num_speculative_steps = 2, has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 2.561972141265869 but got 3.202465057373047.
E                       Absolute difference: 0.6404929161071777 (up to 1e-05 allowed)
E                       Relative difference: 0.2499999534697167 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
____________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-2-8192-4] ____________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192fc20>, num_reqs = 4, vocab_size = 8192, num_speculative_steps = 2, has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.5412683486938477 but got 4.4265851974487305.
E                       Absolute difference: 0.8853168487548828 (up to 1e-05 allowed)
E                       Relative difference: 0.2499999326742411 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
____________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-2-8192-8] ____________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192fcb0>, num_reqs = 8, vocab_size = 8192, num_speculative_steps = 2, has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 4.358977794647217 but got 5.4487223625183105.
E                       Absolute difference: 1.0897445678710938 (up to 1e-05 allowed)
E                       Relative difference: 0.2500000273479919 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
___________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-2-129280-4] ___________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192fdd0>, num_reqs = 4, vocab_size = 129280, num_speculative_steps = 2
has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 4.355048656463623 but got 5.443810939788818.
E                       Absolute difference: 1.0887622833251953 (up to 1e-05 allowed)
E                       Relative difference: 0.25000002737266536 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
___________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-2-129280-8] ___________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192fe60>, num_reqs = 8, vocab_size = 129280, num_speculative_steps = 2
has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.6158158779144287 but got 4.519769668579102.
E                       Absolute difference: 0.9039537906646729 (up to 1e-05 allowed)
E                       Relative difference: 0.24999995054672572 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
___________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-2-248320-4] ___________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff2192ff80>, num_reqs = 4, vocab_size = 248320, num_speculative_steps = 2
has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.424893856048584 but got 4.2811174392700195.
E                       Absolute difference: 0.8562235832214355 (up to 1e-05 allowed)
E                       Relative difference: 0.2500000348067107 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
___________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-2-248320-8] ___________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff21958050>, num_reqs = 8, vocab_size = 248320, num_speculative_steps = 2
has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.894031047821045 but got 4.867538928985596.
E                       Absolute difference: 0.9735078811645508 (up to 1e-05 allowed)
E                       Relative difference: 0.25000003061333825 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
____________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-3-128-4] _____________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff21958230>, num_reqs = 4, vocab_size = 128, num_speculative_steps = 3, has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 2.1012039184570312 but got 2.626504898071289.
E                       Absolute difference: 0.5253009796142578 (up to 1e-05 allowed)
E                       Relative difference: 0.25 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
____________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-3-128-8] _____________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff219582f0>, num_reqs = 8, vocab_size = 128, num_speculative_steps = 3, has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.1271181106567383 but got 3.908897638320923.
E                       Absolute difference: 0.7817795276641846 (up to 1e-05 allowed)
E                       Relative difference: 0.25 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
____________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-3-8192-4] ____________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff21958470>, num_reqs = 4, vocab_size = 8192, num_speculative_steps = 3, has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.4897022247314453 but got 4.362127780914307.
E                       Absolute difference: 0.8724255561828613 (up to 1e-05 allowed)
E                       Relative difference: 0.25 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
____________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-3-8192-8] ____________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff21958530>, num_reqs = 8, vocab_size = 8192, num_speculative_steps = 3, has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 4.467167854309082 but got 5.583959579467773.
E                       Absolute difference: 1.1167917251586914 (up to 1e-05 allowed)
E                       Relative difference: 0.24999994662869476 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
___________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-3-129280-4] ___________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff219586b0>, num_reqs = 4, vocab_size = 129280, num_speculative_steps = 3
has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.3711299896240234 but got 4.213912487030029.
E                       Absolute difference: 0.8427824974060059 (up to 1e-05 allowed)
E                       Relative difference: 0.25 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
___________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-3-129280-8] ___________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff21958770>, num_reqs = 8, vocab_size = 129280, num_speculative_steps = 3
has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 3.641335964202881 but got 4.551670074462891.
E                       Absolute difference: 0.9103341102600098 (up to 1e-05 allowed)
E                       Relative difference: 0.2500000327377893 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
___________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-3-248320-4] ___________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff219588f0>, num_reqs = 4, vocab_size = 248320, num_speculative_steps = 3
has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 4.124239444732666 but got 5.155299186706543.
E                       Absolute difference: 1.031059741973877 (up to 1e-05 allowed)
E                       Relative difference: 0.24999997109544894 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
___________________________________________________________________ TestComputeBlockMaxAndSumexp.test_compute_block_max_and_sumexp[True-3-248320-8] ___________________________________________________________________

self = <accuracy_test.strict_ut.npu.test_compute_local_logits_stats_kernel.TestComputeBlockMaxAndSumexp object at 0xffff219589b0>, num_reqs = 8, vocab_size = 248320, num_speculative_steps = 3
has_draft_logits = True

    @pytest.mark.parametrize("num_reqs", [1, 4, 8])
    @pytest.mark.parametrize("vocab_size", [128, 8192, 129280, 248320])
    @pytest.mark.parametrize("num_speculative_steps", [1, 2, 3])
    @pytest.mark.parametrize("has_draft_logits", [True, False])
    def test_compute_block_max_and_sumexp(
        self,
        num_reqs,
        vocab_size,
        num_speculative_steps,
        has_draft_logits,
    ):
        """Compare block-level max/sumexp/argmax with CPU reference.
    
        Covers:
          - real expansion layout (draft steps + bonus token)
          - bonus early-return branch (outputs untouched)
          - greedy (temp=0) and non-greedy branches per request
          - HAS_DRAFT_LOGITS True/False
          - multi-block vocab (vocab_size > VOCAB_BLOCK_SIZE)
        """
        VOCAB_BLOCK_SIZE = 8192
        vocab_num_blocks = triton.cdiv(vocab_size, VOCAB_BLOCK_SIZE)
    
        total_num_logits, expanded_idx_mapping, expanded_local_pos = \
            _build_real_expansion(num_reqs, num_speculative_steps, self.device)
    
        # Per-request temperature: mix greedy (0.0) and non-greedy.
        # Use a deterministic pattern that exercises both branches when num_reqs>=2.
        temp_values = [0.0, 1.0, 0.8, 0.0, 1.0, 0.8, 0.0, 1.0]
        temperature = torch.tensor(
            temp_values[:num_reqs], dtype=torch.float32, device=self.device
        )
    
        target_logits = torch.randn(
            total_num_logits, vocab_size, dtype=torch.float32, device=self.device
        )
        draft_logits = torch.randn(
            num_reqs, num_speculative_steps, vocab_size,
            dtype=torch.float32, device=self.device,
        )
    
        # Outputs: fill with a sentinel so untouched positions (bonus / greedy
        # sumexp / non-greedy argmax) are distinguishable from real writes.
        sentinel = float("nan")
        target_local_argmax = torch.full(
            (total_num_logits, vocab_num_blocks), -1,
            dtype=torch.int64, device=self.device,
        )
        target_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        target_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_max = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
        draft_local_sumexp = torch.full(
            (total_num_logits, vocab_num_blocks), sentinel,
            dtype=torch.float32, device=self.device,
        )
    
        _compute_local_logits_stats_kernel[(total_num_logits, vocab_num_blocks)](
            target_local_argmax,
            target_local_argmax.stride(0),
            target_local_max,
            target_local_max.stride(0),
            target_local_sumexp,
            target_local_sumexp.stride(0),
            draft_local_max,
            draft_local_max.stride(0),
            draft_local_sumexp,
            draft_local_sumexp.stride(0),
            target_logits,
            target_logits.stride(0),
            draft_logits,
            draft_logits.stride(0),
            draft_logits.stride(1),
            expanded_idx_mapping,
            expanded_local_pos,
            temperature,
            vocab_size,
            num_speculative_steps,
            BLOCK_SIZE=VOCAB_BLOCK_SIZE,
            HAS_DRAFT_LOGITS=has_draft_logits,
        )
        torch.npu.synchronize()
    
        # CPU reference (mirror kernel control flow exactly)
        tgt_cpu = target_logits.cpu()
        drf_cpu = draft_logits.cpu()
        idx_map_cpu = expanded_idx_mapping.cpu()
        local_pos_cpu = expanded_local_pos.cpu()
        temp_cpu = temperature.cpu()
    
        out_argmax = target_local_argmax.cpu()
        out_tmax = target_local_max.cpu()
        out_tsumexp = target_local_sumexp.cpu()
        out_dmax = draft_local_max.cpu()
        out_dsumexp = draft_local_sumexp.cpu()
    
        for li in range(total_num_logits):
            draft_step = local_pos_cpu[li].item()
    
            # --- bonus branch: kernel early-returns, all outputs untouched ---
            if draft_step >= num_speculative_steps:
                for bi in range(vocab_num_blocks):
                    assert out_argmax[li, bi].item() == -1, (
                        f"bonus li={li} argmax should be untouched (-1), "
                        f"got {out_argmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tmax[li, bi].item()), (
                        f"bonus li={li} target_max should be untouched (nan), "
                        f"got {out_tmax[li, bi].item()}"
                    )
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"bonus li={li} target_sumexp should be untouched (nan), "
                        f"got {out_tsumexp[li, bi].item()}"
                    )
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"bonus li={li} draft_max should be untouched (nan), "
                        f"got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"bonus li={li} draft_sumexp should be untouched (nan), "
                        f"got {out_dsumexp[li, bi].item()}"
                    )
                continue
    
            rs = idx_map_cpu[li].item()
            temp = temp_cpu[rs].item()
    
            for bi in range(vocab_num_blocks):
                start = bi * VOCAB_BLOCK_SIZE
                end = min(start + VOCAB_BLOCK_SIZE, vocab_size)
                block_logits = tgt_cpu[li, start:end]
    
                if temp == 0.0:
                    # --- greedy branch: only argmax + target_max written ---
                    expected_max = float(block_logits.max().item())
                    expected_argmax = start + int(block_logits.argmax().item())
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_argmax[li, bi].item(), expected_argmax,
                        rtol=0, atol=0,
                    )
                    # target_sumexp untouched
                    assert math.isnan(out_tsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} target_sumexp should be "
                        f"untouched (nan), got {out_tsumexp[li, bi].item()}"
                    )
                    # draft_* untouched regardless of HAS_DRAFT_LOGITS
                    assert math.isnan(out_dmax[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_max should be "
                        f"untouched (nan), got {out_dmax[li, bi].item()}"
                    )
                    assert math.isnan(out_dsumexp[li, bi].item()), (
                        f"greedy li={li} bi={bi} draft_sumexp should be "
                        f"untouched (nan), got {out_dsumexp[li, bi].item()}"
                    )
                else:
                    # --- non-greedy branch: target_max + target_sumexp ---
                    expected_max, expected_sumexp = _compute_max_and_sumexp_ref(
                        block_logits
                    )
                    torch.testing.assert_close(
                        out_tmax[li, bi].item(), expected_max,
                        rtol=1e-5, atol=1e-5,
                    )
                    torch.testing.assert_close(
                        out_tsumexp[li, bi].item(), expected_sumexp,
                        rtol=1e-5, atol=1e-5,
                    )
                    # argmax untouched in non-greedy branch
                    assert out_argmax[li, bi].item() == -1, (
                        f"non-greedy li={li} bi={bi} argmax should be "
                        f"untouched (-1), got {out_argmax[li, bi].item()}"
                    )
    
                    # draft stats: only when HAS_DRAFT_LOGITS, and computed on
                    # raw draft_logits WITHOUT temperature scaling.
                    # Indexing: draft_logits[rs, draft_step, :]
                    if has_draft_logits:
                        drf_block = drf_cpu[rs, draft_step, start:end]
                        dmax, dsumexp = _compute_max_and_sumexp_ref(drf_block)
>                       torch.testing.assert_close(
                            out_dmax[li, bi].item(), dmax,
                            rtol=1e-5, atol=1e-5,
                        )
E                       AssertionError: Scalars are not close!
E                       
E                       Expected 4.21964168548584 but got 5.274551868438721.
E                       Absolute difference: 1.0549101829528809 (up to 1e-05 allowed)
E                       Relative difference: 0.24999994349790886 (up to 1e-05 allowed)

accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py:310: AssertionError
================================================================================================== warnings summary ===================================================================================================
../../../../../usr/local/python3.12.13/lib/python3.12/site-packages/torch/jit/_script.py:362: 14 warnings
  /usr/local/python3.12.13/lib/python3.12/site-packages/torch/jit/_script.py:362: DeprecationWarning: `torch.jit.script_method` is deprecated. Please switch to `torch.compile` or `torch.export`.
    warnings.warn(

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=============================================================================================== short test summary info ===============================================================================================
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-128-4] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-128-8] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-8192-4] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-8192-8] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-129280-4] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-129280-8] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-248320-4] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-1-248320-8] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-128-4] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-128-8] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-8192-4] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-8192-8] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-129280-4] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-129280-8] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-248320-4] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-2-248320-8] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-128-4] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-128-8] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-8192-4] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-8192-8] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-129280-4] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-129280-8] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-248320-4] - AssertionError: Scalars are not close!
FAILED accuracy_test/strict_ut/npu/test_compute_local_logits_stats_kernel.py::TestComputeBlockMaxAndSumexp::test_compute_block_max_and_sumexp[True-3-248320-8] - AssertionError: Scalars are not close!
===================================================================================== 24 failed, 49 passed, 14 warnings in 12.82s =====================================================================================
